"""Componente unificado de Movistar Prosegur para Home Assistant."""
import logging
from homeassistant.core import HomeAssistant
from homeassistant.helpers.typing import ConfigType

_LOGGER = logging.getLogger(__name__)

DOMAIN = "prosegur_imou"

async def async_setup(hass: HomeAssistant, config: ConfigType) -> bool:
    """Inicialización manual desde configuration.yaml."""
    conf = config.get(DOMAIN)
    if conf is None:
        return True

    _LOGGER.info("Inicializando integración unificada Movistar Prosegur")
    
    # Almacenamos la configuración global dentro del diccionario de datos de HA
    hass.data[DOMAIN] = conf

    return True
